  <div class="activity">
    <% if a.trackable  %>
      <%= activity.owner ? activity.owner.name : 'Guest' %>
      criou um <%= link_to 'reembolso', user_refund_path(@user, a.trackable) %>
    <% else %>
      O reembolso criado foi deletado ):
    <% end %>
  </div>
